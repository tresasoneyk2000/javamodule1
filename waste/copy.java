package statemachine;

public class copy {

	if(notee==50) {
		 no =c2.getN();
		if(no>=n) {
		result=note*n;
		this.amount=this.amount+result;
		c2.setN(no-n);}
		
		if(this.amount==amount) 
			return this.amount ;
		
		
		else 
	       return this.amount;
			
		
		}
	
	if(note==20) {
		 no =c3.getN();
		if(no>=n) {
		result=note*n;
		this.amount=this.amount+result;
		c3.setN(no-n);}
		
		if(this.amount==amount) 
			return this.amount ;
		
		
		else 
	       return this.amount;
			
		
		}
	
	if(note==10) {
		 no =c4.getN();
		if(no>=n) {
		result=note*n;
		this.amount=this.amount+result;
		c4.setN(no-n);}
		
		if(this.amount==amount) 
			return this.amount ;
		
		
		else 
	       return this.amount;
			
		
		}
	
	if(note==5) {
		 no =c5.getN();
		if(no>=n) {
		result=note*n;
		this.amount=this.amount+result;
		c5.setN(no-n);}
		
		if(this.amount==amount) 
			return this.amount ;
		
		
		else 
	       return this.amount;
			
		
		}return this.amount;}
		
		
	
	
	
}
	if(result<amount) {
	 System.out.println("Number of twenty rupees:");
	 n=sc.nextInt();
	 note=20;
	 result= m.withdraw(note, n, amount);}
	System.out.println(result);
	if(result<amount) {
	 System.out.println("Number of ten rupees:");
	 n=sc.nextInt();
	// n=sc.nextInt();
	 note=10;
	 result= m.withdraw(note, n, amount);}
	System.out.println(result);
	if(result<amount) {
	 System.out.println("Number of five rupees:");
	 n=sc.nextInt();
	 note=5;
	 result= m.withdraw(note, n, amount);}
	System.out.println(result);
}
